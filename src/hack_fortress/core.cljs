(ns hack_fortress.core
  (:require [hack-fortress.planning.test-interface]))

;

;(defn on-js-reload []
  ;    (redraw!)
  ;(println "oh hi")
  ;)
;
;
;
;(defn make-move! []
;  (if (= (:state @s/game-state) :running)
;    (swap! s/game-state ai/make-move))
;  ;(println app-state)
;  (redraw!))